using System;

class ConsoleappArrayAssignment {
    // This is the main entry point for the application.
    
    static void Main () {
        string[] names = { Jon, Doe, Jane, Smith, Alice };
        // Initializing an array of names
        // Ask the user to select an index of the Array
        Console.WriteLine("Please select an index of the Array (0-4):");
        Console.ReadLine();
        // Read the user input
    }
}